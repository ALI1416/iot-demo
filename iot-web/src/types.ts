/**
 * 通用类型
 */

/**
 * 报表类型
 * MONTH月 DAY日 HOUR小时 MINUTE分钟
 */
type ReportType = 'MONTH' | 'DAY' | 'HOUR' | 'MINUTE'

/**
 * 时间类型
 * YEAR年 MONTH月 DAY日 HOUR小时
 */
type DateType = 'YEAR' | 'MONTH' | 'DAY' | 'HOUR'

export type {ReportType, DateType}
